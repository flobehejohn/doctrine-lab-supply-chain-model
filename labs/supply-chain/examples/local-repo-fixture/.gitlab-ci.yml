stages:
  - build

build:image:
  stage: build
  image: docker:27.5.1-cli
  services:
    - name: docker:27.5.1-dind
      command: ["--tls=false"]
  variables:
    DOCKER_HOST: tcp://docker:2375
  script:
    - docker build -t local-repo-fixture:$CI_COMMIT_SHORT_SHA .
